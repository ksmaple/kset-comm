# Gateway 组件配置

# 来源：KSet Gateway Starter / Spring Cloud Gateway。
# 说明：引入 kset-starter-gateway 后默认启用网关过滤器、CORS、灰度标签透传。
# 非必选项：需要覆盖网关行为时再打开。
# kset:
#   cloud:
#     gateway:
#       enabled: true
#       route-data-id: ${spring.application.name}-routes
#       sentinel-enabled: true
#       auth-enabled: false
#       cors-enabled: true
#       auth-token-header: X-Auth-Token
#       trace-header: X-Trace-Id
#       gray-header: X-Gray-Tag

# 来源：Spring Cloud Gateway 原生配置。
# 非必选项：静态路由可直接使用 spring.cloud.gateway.routes；动态路由优先使用 Nacos dataId。
# spring:
#   cloud:
#     gateway:
#       discovery:
#         locator:
#           enabled: true
#           lower-case-service-id: true
#       httpclient:
#         connect-timeout: 3000
#         response-timeout: 5s
#       default-filters:
#         - StripPrefix=1
#       routes:
#         - id: demo-user-service
#           uri: lb://demo-user-service
#           predicates:
#             - Path=/user/**
#           filters:
#             - StripPrefix=1
#       globalcors:
#         cors-configurations:
#           '[/**]':
#             allowed-origin-patterns:
#               - "*"
#             allowed-methods:
#               - GET
#               - POST
#               - PUT
#               - DELETE
#               - OPTIONS
#             allowed-headers:
#               - "*"
#             allow-credentials: true
