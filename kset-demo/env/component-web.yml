# Web 组件配置

# 来源：KSet Web Starter。
# 说明：引入 kset-starter-web 后，全局异常、ApiResponse、traceId 响应等默认自动装配。
# 非必选项：需要覆盖 Web 行为时再打开。
# kset:
#   web:
#     oplog:
#       enabled: true
#       user-id-header: X-User-Id
#     request-logging:
#       enabled: false
#     response:
#       trace-id-enabled: true

# 来源：Spring MVC / Servlet 原生配置。
# 非必选项：需要覆盖路径匹配、请求大小、错误响应时再打开。
# spring:
#   mvc:
#     pathmatch:
#       matching-strategy: path_pattern_parser
#   servlet:
#     multipart:
#       max-file-size: 10MB
#       max-request-size: 20MB
# server:
#   servlet:
#     context-path: /
#   error:
#     include-message: always
#     include-binding-errors: always
